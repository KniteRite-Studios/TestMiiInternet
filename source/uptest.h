#include <stdint.h>

// Function to perform upload speed test with timeout
size_t upload_with_timeout(int timeout_seconds);

// Function to retrieve upload time (similar to download test)
uint32_t retrieve_up_time(void);
